# 하방 변동성 사이징 (외부 자료)

2026-09-03 에 외부에서 받은 자료. **이 저장소의 봇은 이걸 쓰지 않는다.**

원본은 저장소 루트에 있었는데 두 가지 이유로 여기로 옮겼다.
- `CLAUDE.md` 는 루트에 있으면 Claude Code 가 *이 프로젝트*의 지침으로 읽는다.
  내용은 다른 프로젝트 컨텍스트라 혼선을 준다. `CONTEXT.md` 로 이름을 바꿨다.
- 루트 `requirements.txt` 가 `freqtrade/dashboard/`, `tools/` 의 것들과 겹쳤다.

## 검증 결과 (2026-09-03, 이 저장소 데이터로 독립 재현)

선물 일봉 기준, 고정레버 대조군 대비 Sharpe 개선폭:

| | 결과 |
|---|---|
| 8개 자산 재현 | 8/8 양수, 평균 +0.066 |
| 개별 유의성 | 2/8 만 p<0.05, 나머지는 CI 가 0 을 포함 |
| 실효 독립 표본수 | 자산간 초과수익 상관 0.486 -> **약 1.8개** (8개가 아님) |
| 국면 의존성 | 상승장 8/8 · 6/8, 하락장 2/8 · 1/8 |

**재현은 되지만 통계적으로 확립되지 않았고, 우위가 상승장에 몰려 있다.**
저자 본인도 원문에 같은 내용을 적어뒀다.

봇에 스테이크 사이징으로 붙여본 결과는 PF 1.015 -> 1.040 으로,
방향은 맞지만 손익을 바꿀 크기가 아니었다.
